{
  "name": "test-electron",
  "version": "1.0.0",
  "private": true,
  "main": "packages/main/dist/main.js",
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "npm run build -w packages/main && npm run build -w packages/renderer && node launch.js",
    "pack": "npm run build -w packages/main && npm run build -w packages/renderer && node pack.js"
  },
  "devDependencies": {
    "electron": "^32.0.0",
    "electron-packager": "^17.1.2",
    "typescript": "^5.4.0"
  }
}
